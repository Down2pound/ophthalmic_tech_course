#!/usr/bin/env node

const requiredTables = [
  "commerce_purchases",
  "commerce_enrollments",
  "commerce_practice_seat_packs",
  "commerce_practice_seat_assignments",
  "commerce_practice_inquiries",
  "auth_users",
  "auth_magic_links",
  "auth_sessions",
  "learning_lesson_completions",
  "assessment_attempts",
  "assessment_question_results",
];

const lines = [
  "# OptiTech Academy Database Setup",
  "",
  "Use this before paid enrollment opens. The database is the app's long-term memory for purchases, learner access, practice seats, sign-in sessions, lesson progress, and quiz attempts.",
  "",
  "Paid checkout intentionally fails closed until the production database is configured and the launch schema is verified.",
  "",
  "Do not paste database passwords, raw connection strings, SQL dumps with learner emails, session cookies, raw sign-in tokens, patient information, protected health information, Stripe secrets, or email API keys into this checklist.",
  "",
  "## Host Dashboard Values",
  "",
  "Set these in Render or your production host dashboard:",
  "",
  "```text",
  "DATABASE_URL=",
  "DATABASE_SSL=true",
  "```",
  "",
  "Most managed PostgreSQL providers require SSL. Keep `DATABASE_SSL=true` unless your provider clearly says otherwise.",
  "",
  "## Setup Command",
  "",
  "Run this only after `DATABASE_URL` points to the real managed PostgreSQL database:",
  "",
  "```bash",
  "pnpm db:setup",
  "```",
  "",
  "The setup uses `CREATE TABLE IF NOT EXISTS`, so it is safe to run again while checking a launch candidate.",
  "",
  "## Tables The App Must See",
  "",
  ...requiredTables.map(table => `- \`${table}\``),
  "",
  "## Setup Order",
  "",
  "1. Create the managed PostgreSQL database.",
  "2. Add `DATABASE_URL` to the production host dashboard.",
  "3. Set `DATABASE_SSL=true`.",
  "4. Run `pnpm db:setup` against the production database.",
  "5. Restart or redeploy the app if the host does not reload environment values automatically.",
  "6. Open `/api/launch/readiness`.",
  "7. Confirm the database schema is verified and no required tables are missing.",
  "",
  "## What To Prove Before Selling",
  "",
  "- `pnpm db:setup` completed against the production database.",
  "- `/api/launch/readiness` reports the launch database schema as verified.",
  "- Stripe test checkout created a durable individual enrollment.",
  "- Stripe test checkout created a durable practice seat pack.",
  "- A custom practice inquiry created a durable lead record.",
  "- A learner's lesson progress survives refresh and sign-out/sign-in.",
  "- A quiz attempt is saved server-side.",
  "",
  "## If Readiness Says Tables Are Missing",
  "",
  "1. Confirm `DATABASE_URL` points to the intended production database.",
  "2. Confirm the host can reach the database network.",
  "3. Confirm the database user can create tables and indexes.",
  "4. Run `pnpm db:setup` against the same database used by the deployed app.",
  "5. Recheck `/api/launch/readiness` after the app restarts or reloads environment values.",
  "",
  "Detailed guide: docs/launch/database-setup-guide.md",
  "",
];

console.log(lines.join("\n"));
