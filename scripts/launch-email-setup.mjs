#!/usr/bin/env node

const lines = [
  "# OptiTech Academy Email Setup",
  "",
  "Use this before paid enrollment opens. Buyers can pay through Stripe, but they still need email to receive passwordless sign-in links and welcome messages.",
  "",
  "The simplest production path is Resend because the app already sends a Resend-compatible JSON body.",
  "",
  "Do not paste email API keys, raw sign-in links, magic-link tokens, session cookies, database passwords, patient information, protected health information, or private learner details into this checklist.",
  "",
  "## Host Dashboard Values",
  "",
  "Set these in Render or your production host dashboard:",
  "",
  "```text",
  "AUTH_SESSION_SECRET=",
  "TRANSACTIONAL_EMAIL_API_URL=https://api.resend.com/emails",
  "TRANSACTIONAL_EMAIL_API_KEY=",
  "SIGN_IN_FROM_EMAIL=OptiTech Academy <noreply@your-domain.example>",
  "PUBLIC_APP_URL=https://your-domain.example",
  "```",
  "",
  "Never commit real values to GitHub. Keep them only in the host dashboard.",
  "",
  "## Message Shape The App Sends",
  "",
  "The app sends both sign-in emails and purchase welcome emails with this shape:",
  "",
  "```json",
  JSON.stringify(
    {
      from: "OptiTech Academy <noreply@your-domain.example>",
      to: "learner@example.com",
      subject: "Your OptiTech Academy sign-in link",
      text: "Use this secure link to sign in to OptiTech Academy...",
      html: "<p>Use this secure link to sign in to OptiTech Academy...</p>",
    },
    null,
    2
  ),
  "```",
  "",
  "## Provider Setup Checklist",
  "",
  "1. Create or open a Resend account.",
  "2. Verify the sender domain or sender address.",
  "3. Create a server-side API key.",
  "4. Set `TRANSACTIONAL_EMAIL_API_URL=https://api.resend.com/emails`.",
  "5. Set `TRANSACTIONAL_EMAIL_API_KEY` only in the production host dashboard.",
  "6. Set `SIGN_IN_FROM_EMAIL` to the verified sender.",
  "7. Confirm `PUBLIC_APP_URL` is the real deployed HTTPS domain.",
  "8. Keep `ENABLE_PAID_ENROLLMENT=false` until sign-in and welcome email tests pass.",
  "",
  "## What To Test Before Selling",
  "",
  "- A test learner can request a sign-in email.",
  "- The sign-in email arrives in the inbox, or you understand spam/quarantine behavior.",
  "- The sign-in link starts with the production `PUBLIC_APP_URL`.",
  "- The sign-in link opens `/learn` and creates a session.",
  "- The same sign-in link cannot be reused.",
  "- Expired or invalid links show a safe error.",
  "- A Stripe test purchase triggers the welcome email path.",
  "",
  "## If Email Fails",
  "",
  "Check these in order:",
  "",
  "1. `/api/launch/readiness` shows email configuration is ready.",
  "2. `TRANSACTIONAL_EMAIL_API_URL` is the correct provider endpoint.",
  "3. `TRANSACTIONAL_EMAIL_API_KEY` is active and server-side only.",
  "4. `SIGN_IN_FROM_EMAIL` is verified by the provider.",
  "5. `PUBLIC_APP_URL` uses the deployed HTTPS domain.",
  "6. The learner used the same email that received purchase access.",
  "7. The email provider did not suppress, bounce, or quarantine the message.",
  "",
  "Detailed guide: docs/launch/email-setup-guide.md",
  "",
];

console.log(lines.join("\n"));
